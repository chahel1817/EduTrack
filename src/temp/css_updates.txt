1. Add dark mode style for .hero-pro-title:

Search:
.hero-pro-title {
  font-size: 36px;
  font-weight: 800;
  margin: 0;
  background: linear-gradient(135deg, #ffffff, rgba(255,255,255,0.9));
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  transition: color 0.3s ease;
}

Replace with:
// (Keep above block unchanged. This is just the search marker for locating place to insert below)

Add after this block:

body.dark .hero-pro-title {
  background: linear-gradient(135deg, #f5a623, #ff6f61);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

2. Adjust colors of .table-container th and td in light mode

Search:
.table-container th {
  background: var(--gray-100);
  padding: 16px 20px;
  text-align: left;
  font-weight: 600;
  color: var(--black);
  font-size: 14px;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

Replace with:

.table-container th {
  background: var(--gray-100);
  padding: 16px 20px;
  text-align: left;
  font-weight: 600;
  color: #1a1a1a; /* Darker black for better contrast */
  font-size: 14px;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

Search:
.table-container td {
  padding: 16px 20px;
  border-bottom: 1px solid var(--gray-200);
  color: var(--gray-700);
  font-size: 14px;
}

Replace with:

.table-container td {
  padding: 16px 20px;
  border-bottom: 1px solid var(--gray-200);
  color: #333333; /* Darker gray for better clarity */
  font-size: 14px;
}

3. Adjust hover background for .table-container tr:hover in light mode.

Search:
.table-container tr:hover {
  background: var(--gray-50);
}

Replace with:

.table-container tr:hover {
  background: #f0f4ff; /* Subtle friendly blue tone */
  cursor: pointer;
}
